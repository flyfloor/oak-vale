-@user ||= current_user
.stats
	%a{href: "#{following_user_path(@user)}"} 
		%span 关注
		%strong.stat#following #{@user.followed_users.count}
	%a{href: "#{followers_user_path(@user)}"}
		%span 被关注
		%strong.stat#followers #{@user.followers.count}
