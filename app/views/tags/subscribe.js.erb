var $sub_btn = $("#<%= @tag.id %>");
if ($sub_btn.hasClass("subscribed")) {
	$sub_btn.removeClass("subscribed").html("<%= escape_javascript(render('tags/subscribe')) %>")
}else{
	$sub_btn.addClass("subscribed").html("<%= escape_javascript(render('tags/unsubscribe')) %>")
}
