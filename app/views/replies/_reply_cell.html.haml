.field
	=link_to model.user.name, user_path(model.user)
	%time=timeago(model.created_at)
=model.content
=link_to '回复', 'javascript:;', data: {name: model.user.name}, class: 'reply-btn'
.action
	-if current_user? model.user
		=link_to 'x', [model.topic.group, model.topic, model], method: :delete
