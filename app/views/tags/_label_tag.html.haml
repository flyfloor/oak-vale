%h4.italic 贴标签分个类吧！
.tag-picker
	.selected-tags
		.tag-input
			%input#new-tag{type:'text', placeholder: 'Add a label'}
		%input#hid_tag{type: 'hidden', name: "tags"}
		%input#hid_swap{type: 'hidden'}
		-if model.tags.present?
			-model.tags.each do |tag|
				%tag.tag=tag.name
	.exist-tags
		%p.italic All existed tags	
		-Tag.all.each do |tag|
			%tag.ext-tag=tag.name
